type Course {
    _id: ID!
    title: String!
    teacher: String
    description: String!
    topic: String
}

type Query {
    "Devuelve todos los cursos"
    Courses: [Course!]
    "Devuelve un curso"
    Course(id: ID!): Course
}